import React,{useRef,useState}from'react';import{createRoot}from'react-dom/client';import'./popup.css';
function App(){const i=useRef<HTMLInputElement>(null);const[b,setB]=useState(false);const [m,setM]=useState('');const open=()=>i.current?.click();const run=async(f:File)=>{setB(true);setM('Reconstructing…');try{const fd=new FormData();fd.append('file',f);const r=await fetch('http://localhost:8000/v1/reconstruct',{method:'POST',body:fd});if(!r.ok)throw 0;const j=await r.json();await chrome.storage.local.set({aedom:j.document});chrome.tabs.create({url:'http://localhost:3000/editor'}) ;setM('Done')}catch{setM('Could not connect to API')}finally{setB(false)}};return <div className="popup"><div className="brand"><b>AE</b><span>Anything Editable</span></div><button onClick={open} className="primary">{b?'Working…':'Make Editable'}</button><button onClick={()=>chrome.tabs.create({url:'http://localhost:3000'})} className="secondary">Open web app</button><button onClick={open} className="secondary">Capture / upload screenshot</button><input ref={i} hidden type="file" accept=".pdf,.png,.jpg,.jpeg,.webp" onChange={e=>e.target.files?.[0]&&run(e.target.files[0])}/><small>{m||'PDF, images and screenshots'}</small></div>}createRoot(document.getElementById('root')!).render(<App/>);
